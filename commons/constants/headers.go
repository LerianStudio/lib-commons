package constant

const (
	// HeaderUserAgent represents the User-Agent HTTP header.
	HeaderUserAgent     = "User-Agent"
	// HeaderRealIP represents the X-Real-Ip HTTP header.
	HeaderRealIP        = "X-Real-Ip"
	// HeaderForwardedFor represents the X-Forwarded-For HTTP header.
	HeaderForwardedFor  = "X-Forwarded-For"
	// HeaderForwardedHost represents the X-Forwarded-Host HTTP header.
	HeaderForwardedHost = "X-Forwarded-Host"
	// HeaderHost represents the Host HTTP header.
	HeaderHost          = "Host"
	// DSL represents the domain-specific language identifier.
	DSL                 = "dsl"
	// FileExtension represents the gold file extension.
	FileExtension       = ".gold"
	// HeaderID represents the X-Request-Id HTTP header.
	HeaderID            = "X-Request-Id"
	// IdempotencyKey represents the X-Idempotency HTTP header.
	IdempotencyKey      = "X-Idempotency"
	// IdempotencyTTL represents the X-TTL HTTP header.
	IdempotencyTTL      = "X-TTL"
	// Authorization represents the Authorization HTTP header.
	Authorization       = "Authorization"
	// Basic represents the Basic authentication method.
	Basic               = "Basic"
	// BasicAuth represents the Basic Auth authentication method.
	BasicAuth           = "Basic Auth"
	// WWWAuthenticate represents the WWW-Authenticate HTTP header.
	WWWAuthenticate     = "WWW-Authenticate"
)

// Package schema defines the canonical Dictionary types emitted by the
// telemetry-inventory CLI.
package schema

// SchemaVersion is the current Dictionary schema version.
const SchemaVersion = "1.0.0"
